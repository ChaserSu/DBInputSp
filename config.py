# 加载双拼方案格式：编号（≥1的正整数）+空格+双拼方案（method目录下文件名，不含“py”扩展名）
1 xiaohe
2 ziranma

# 启动时加载的默认方案编号（≥1的正整数）
default=1

# 历史记录功能（仅1/0，“1为开启”，“0”为关闭，会在同目录下自动创建“history.txt”的文件）
history=1

# 历史记录清除行数（正整数，“history.txt”文件超过多少行，自动清空内容，设为“0”不清空）
clrhis=1000
